packages:
  - web

# pnpm 10+ 默认不执行依赖的 postinstall 脚本；构建工具依赖需在此白名单放行
onlyBuiltDependencies:
  - '@tailwindcss/oxide'
  - esbuild

# 强制 flowbite-svelte 使用无 slots 缓存 bug 的 tailwind-variants 3.3.1（3.3.0 的 disabled 变体 class 不更新）
overrides:
  tailwind-variants: 3.3.1
